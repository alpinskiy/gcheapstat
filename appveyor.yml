version: '0.1.{build}'
image: Visual Studio 2019
install: git submodule update --init --recursive
before_build:
  - echo on
  - dir
  - type src\version.h
  - break src\version.h
  - type src\version.h
build_script: appveyor.py
